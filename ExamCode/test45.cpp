#include <stdio.h>
#include <math.h>

int main()
{
    int x;
    float f;
    printf("Input an integer:");
    scanf("%d",&x);
    //f = (fabs(x)-2)/(pow(x,2)+1);
    printf("F(x)=%f\n",f);
    return 0;
}